class Solution:
    def plusOne(self, digits: List[int]) -> List[int]:
        s=""
        for i in digits:
            s+=str(i)
        m=int(s)+1
        arr=[]
        for i in str(m):
            arr.append(int(i))
        return arr
        